You are an expert in information retrieval.

Task:
Given a user interest description, generate a diverse set of keyword-based search queries suitable for a lexical keyword search engine such as Whoosh/BM25

Instructions:
1. Analyze the user interests and identify:
   - main topics
   - subtopics
   - relevant entities (people, organizations, locations)
   - related concepts and synonyms

2. Generate between 10 and 25 search queries that:
   - use keywords and short phrases (NOT full sentences)
   - are 2–6 terms long
   - include alternative phrasings and closely related terminology when useful
   - include specific entities when relevant
   - optionally use quoted phrases for exact matches
   - vary in specificity (broad to narrow)
   - prefer meaningful content words over generic words.
   - do not include temporal expressions such as "today", "this week", or "latest".

3. Ensure diversity:
   - avoid redundant or near-duplicate queries
   - cover different aspects and angles of the interests
   - include both general and niche queries

4. Output format:
   - Return ONLY a JSON array of strings
   - Do NOT include explanations or any extra text

User interests:
"""
{interest_description}
"""